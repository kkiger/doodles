Treehouse Javascript Notes

var my_array = [2, 3, 4];
console.log(my_array.toString());

//adds 5 to end of array
my_array.push(5)
console.log(my_array.toString());

.toString() makes it so if you have a console.log soon
after printing the array, it will print the array properly
with only the 3 exisiting values versus printing the first
log and second log the same